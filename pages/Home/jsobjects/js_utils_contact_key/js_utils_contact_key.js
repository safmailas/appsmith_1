export default {
	async update () {
		// if (pin.text == "2583") {
			// const key_id = await js_utils_rfid.hexToDecimal(rfid_update.text)}
			//const key_id = checkbox_scan_keys.isChecked ? "upsert" : "select"
	}
}